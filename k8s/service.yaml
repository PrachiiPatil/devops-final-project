apiVersion: v1
kind: Service
metadata:
  name: tic-tac-toe-svc
  labels:
    app: tic-tac-toe
spec:
  type: NodePort       
  selector:
    app: tic-tac-toe
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080 
